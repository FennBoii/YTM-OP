name: Run EXE

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  run_exe:
    runs-on: windows-2022
    steps:
    - uses: actions/checkout@v3
    - name: Run Executable
      run: |
        cd dist
        ./YTM-OP-Setup-1.0.7.exe
