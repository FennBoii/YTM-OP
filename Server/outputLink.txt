
                <html>
                    <body>
                        <h1>This is your New Home Page!, FennBoii!</h1>
                        <img id="newImg" src="/userRedirects/FennBoii/FennBoii.png">
                
                        <script>
                            var currentVersion = 0; // Initial version
                
                            setInterval(function() {
                                var img = document.getElementById("newImg");
                
                                // Fetch the new version number from the server (you can use AJAX here)
                                var newVersion = 0; // Replace this with the actual code to fetch the new version number
                
                                if (newVersion !== currentVersion) {
                                    currentVersion = newVersion;
                                    img.src = "/userRedirects/FennBoii/FennBoii.png?v=" + currentVersion;
                                }
                            }, 5000); // Check for updates every 5 seconds
                        </script>
                    </body>
                </html>