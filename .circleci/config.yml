version: 2.1
orbs:
  node: circleci/node@5.1.0
jobs:
  example-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: apt-get update && apt-get upgrade

workflows:
  example-workflow:
    jobs:
      - example-job